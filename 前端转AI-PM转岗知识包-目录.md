# 前端转 AI PM 转岗知识包

> 诞生于 2026-08-29 的一天高强度问答。起点：前端开发；目标：AI 产品经理（湖北某军区智慧仓储项目三期 Agent）。
> 用法：按第二节路径走完 → 第四节终验 10 题 → 全部脱口而出，出师。

---

## 一、知识包结构（6+1 份材料）

| # | 材料 | 解决的问题 | 核心一句话 |
|---|------|-----------|-----------|
| 0 | 本目录 | 导航与速查 | 知识包的 README |
| 1 | skill万能模板.md | skill 怎么写 | 四层：感知/执行/兜底/附件——不多写一个字，不少写一个字 |
| 2 | skill案例-电商退款申请.md | 看标准答案 | 对照模板的完整示范（电商退款 skill） |
| 3 | skill-物资领用（智慧仓储）.md | 落到真实项目 | 领用≠借用 · 品类分流 · 身份前置 · 8 条兜底 |
| 4 | Agent五层架构学习手册.md | 从文件到系统 | 感知/规划/记忆/工具/执行 + 横切风控；解开三个"同名不同物" |
| 5 | AI产品经理项目全记录-故事版.md | 全程怎么干、跟谁打交道 | 六字工作流：听切定给验讲 |
| 6 | Prompt编写手册-六节点模板.md | prompt 怎么写、给谁写 | 内容（说什么）PM 定，工程（怎么稳定做到）算法做 |

---

## 二、学习路径（六步 · 约一周）

**第 1 步 · 会写 skill（Day 1-2）**
读 1+2。练手：挑一个你熟悉的业务，照万能模板写一个 skill，写完对照自查清单逐条过。

**第 2 步 · 落到项目（Day 2-3）**
读 3。练手：照领用 skill 的标准，把入库、出库、借用、盘点四个补齐——这是最好的转岗练习，没有之一。

**第 3 步 · 拔到系统（Day 3-4）**
读 4。重点啃第一节三个"同名不同物"（Agent感知层≠Skill感知层、记忆层≠RAG、工具层≠执行层）和第二节逐层拆解；做手册第六节自测 7 题。

**第 4 步 · 进入协作（Day 4-5）**
读 5+6。故事版用三遍读法（通读代入 → 只看每幕【复盘】→ 合上文档口述三个冲突场景）；prompt 手册练手：给意图识别节点写一份需求单（正例/边界例/反例各 3 条）。

**第 5 步 · 打通运行时（Day 5-6）**
读本目录第三节 Q5，能白板画出"领 20 张靶纸"四轮调用的上下文包变化：门牌索引 → skill 正文进包 → 工具结果进包 → 输出格式进包。

**第 6 步 · 终验（Day 6-7）** → 第四节 10 题，全部脱口而出。

---

## 三、六问六答速查（你的疑问链，也是认知升级链）

**Q1 感知层我产出什么？prompt、skill 还是文档？**
→ 都不是。产出三类：**判定标准**（意图体系 / 置信度三档 / 拒识规则）、**数据资产**（评测集 / 别名字典）、**验收机制**（指标 / 发版闸门）。你写的正反例会被算法搬进 prompt 做 few-shot——**你产原料和标准，算法产实现**。
→ "意图识别用什么框架"五段式答法：结论 → 约束（涉密私有化 / 意图少 / 零标注）→ 演进（few-shot → 评测集 → 小模型前置）→ 证据（准确率 / 误路由率）→ 分工。

**Q2 整个项目我的工作流程？怎么跟开发、算法、客户协作？**
→ 六字：**听**（调研，问"上周最烦的事"）→ **切**（分期，每期带验收数字）→ **定**（标准与规则：状态机 / 意图边界 / 阈值 / 闸门）→ **给**（原料与数据：正反例 / 评测集）→ **验**（评测与发版闸门）→ **讲**（对客户复盘，用数字和可控性演示）。
→ 三种语言：对客户讲痛点和可控性；对算法给边界不碰模型；对开发给契约像联调。详见故事版。

**Q3 哪些节点写 prompt？小公司谁写？意图和 skill 什么关系？**
→ 6 个节点：系统 prompt、意图识别、参数抽取、RAG 问答、回复生成、skill 门牌（这个 PM 直接写）。
→ 小公司 PM 全写很常见，**分界线在顺序：先标准后翻译是 PM 思维，直接调 prompt 是玄学调参**。
→ 意图≠skill：9 意图分三去向（5 业务 skill、1 工具直调、3 RAG）；配 skill 的标准 = **状态变更 + 多步校验 + 需要兜底**。

**Q4 prompt 具体怎么写、怎么交给算法？**
→ 五段骨架：角色 / 任务 / 规则 / 示例 / 输出格式 = PRD 的定位 / 用户故事 / 红线 / 验收用例 / 接口契约。
→ 交的是**内容需求单**，不是 prompt 成品。评审三检查点：红线用硬约束词、示例三类齐（正 / 边界 / 反）、输出格式封死。
→ 六坑：散文式、规则写两处、只给正例、没定输出格式、一次改多个变量、弱约束词。

**Q5 大模型怎么执行 prompt 和 skill？怎么触发？**
→ **模型不执行任何东西**——无状态纯函数 f(上下文)→文本，一切动作是代码干的。
→ 触发 = Function Calling 循环：代码组装上下文（系统 prompt + skill 门牌 + 历史 + 输入）→ 模型返回纯文本（本轮结束）或工具调用 JSON → 代码执行（加载 skill 正文 / 调 WMS / 检索 RAG）→ 结果追加上下文 → 再调模型 → 循环直到出现纯文本。
→ 一次"领 20 张靶纸" = 模型被调 4 次：意图识别轮（只看门牌）→ 参数抽取轮（skill 正文进包）→ 校验执行轮（工具结果进包）→ 输出轮（输出格式进包）。
→ 前端类比：模型=无状态纯函数；上下文=每次全量重传的 props；Agent 循环=事件循环；skill 懒加载=动态 import。

**Q6 三个"永远记住"（防认知回退）**
→ 模型不记得做过什么（记忆靠工程代码写回上下文 / 记忆层）
→ 确认按钮不经过模型（写操作最后一步是代码直接调业务接口）
→ "执行"永远是代码，模型只是决策者和话术生成器

---

## 四、终验 10 题（全部脱口而出 = 出师）

1. skill 四层和 Agent 五层分别是什么？什么关系？（skill 是规划层的剧本）
2. Agent 感知层和 skill 感知层的区别？（系统前台 vs 文件门牌）
3. RAG 在五层架构的哪层？记忆层为什么不能直接生成正式单据？（工具层只读工具；记忆只能参考）
4. 领用和借用的业务边界一句话？（借须归还、占用在册；领是消耗/责任移交、出账）
5. 箱式货物和散货在领用 skill 里的校验差异？（审批单 vs 月度限额）
6. 工具层为什么默认全只读？写操作怎么落地？（人工确认后走业务系统原生接口）
7. 一次"领 20 张靶纸"模型被调用几次？每轮上下文包的差异？
8. skill 的 description 为什么控制在 100 字？（常驻每次调用的上下文 = 常驻 token 开销）
9. bad case"领用识别成借用"改哪里？（skill 感知层 description 负面边界 / 意图体系）
10. 给军队客户演示，演示什么比演示功能更有效？（可控性：现场拒绝演示 + 审计留痕）

---

## 五、面试速查（三个口径直接背）

**30 秒 · skill 方法论**
"优秀的企业级 skill 是四层信息架构：感知层用 name/description 做索引，100 字内说清触发条件与负面边界；执行层用输入规范、执行逻辑、输出格式定义能力边界；兜底层用'条件→动作'一行一条锁定下限；附件层只挂路径、按需加载。PM 的价值：用精准描述控制 token、用边界定义控制行为、用兜底策略锁定下限——这三件事研发不会主动做。"

**60 秒 · 五层架构 + 项目证据**
"Agent 分层架构我从产品能力视角主导：感知层定三档置信度阈值；规划层的调度剧本就是我写的业务 skill——领用的槽位追问和'权限→库存→限额'校验编排；记忆层定存储黑白名单和生命周期，RFID 码值、涉密参数绝对不存；工具层默认全只读，写操作必须过执行层的人工确认闸门、再由 WMS 原生接口执行；风控合规层全链路审计。上线后用 bad case 评测集持续迭代——误调用改 description，异常失控补兜底。涉密场景必须每层有边界、全链路可审计。"

**90 秒 · 三个冲突场景**（详见故事版末尾）
三期说服（客户想一步上 AI）→ 角色边界（算法问"借和领怎么分"）→ 闸门拦版（评测不过不许发）。共同收尾："AI PM 的核心产出是三层东西：让 Agent 不会走错的标准、出了错能兜住的规则、每次迭代有据可依的评测机制。"

---

## 六、还欠的账（练手清单）

- [ ] 入库 / 出库 / 借用 / 盘点四个 skill（照领用 skill 标准）
- [ ] 意图分类体系正式文档（9 意图，正例 / 边界例 / 反例各 3 条）
- [ ] 评测集模板 + bad case 台账模板
- [ ] 下一站：智能靶场 / 装备库复用方案的 skill 组设计

---

*附：本知识包的前端母语——接口契约=意图体系 · 测试用例=评测集 · bug 单=bad case 流转 · 联调分工=PM/算法协作 · 动态 import=skill 懒加载 · 无状态组件=模型。*
