# Agent 五层架构 · 智慧仓储项目落地学习手册

> 目标：把架构从"AI 给我的文档"变成"自己随手能讲的知识"。
> 用法：每层按【一句话 → 管什么/绝不管 → 项目对应 → 你要定什么 → 疑惑 Q&A】五步走；最后用第六节自测清单验收。
> 配套文件：《skill万能模板.md》《skill-物资领用（智慧仓储）.md》

## 〇、先建立心智模型

把 Agent 想象成仓库新来的一个"聪明的兵"：

| 层 | 比喻 | 一句话 |
|---|---|---|
| 感知层 | 耳朵和眼睛 | 听懂"帮我领 20 张靶纸"是什么意思 |
| 规划层 | 大脑 | 决定走哪个流程、缺什么补什么、错了怎么办 |
| 记忆层 | 记事本 | 记住本次对话和这个单位/人的历史习惯 |
| 工具层 | 手脚 | 去查库存、去调接口——写操作要经批准 |
| 执行层 | 嘴巴+印章 | 汇报结果；盖章（出库）前必须经人确认 |
| 风控合规层 | 纠察 | 全程盯着每层不越界，事事留痕 |

总纲一句话：**每层只回答三个问题——管什么？绝不管什么？和上下层怎么交互？**

---

## 一、先解开三个最容易混的概念（疑惑的最可能来源）

### 1. Agent 感知层 ≠ Skill 感知层（同名不同物）
- **Agent 感知层**是系统级前台：把口语变成结构化信号（意图=领用、实体=靶纸×20、置信度=0.91）
- **Skill 感知层**是文件级门牌：skill 头部的 name/description，100 字以内
- **关系**：前台识别完意图后，靠读各 skill 的"门牌"决定进哪个门。门牌写不准（description 模糊），前台再聪明也走错门——这就是"误调用的幻觉根本原因"在架构里的位置

### 2. 记忆层 ≠ RAG 知识库
- **记忆层**是动态的，跟着"人和会话"走：本次对话填了什么槽、这个单位上月领了多少靶纸
- **RAG 知识库**是静态的，跟着"制度和文档"走：排障手册、保养规范、仓储制度
- **架构归属**：RAG 检索本质是**工具层的一个只读工具**，被规划层在知识问答意图时调度；记忆层才是独立的一层
- 一句话：**记忆是经历，知识库是教材**。战士问"E03 怎么排障"查教材；Agent 记得"他昨天借的雷达还没还"靠经历

### 3. 工具层 ≠ 执行层
- **工具层**管"调用外部系统"：库存查询、权限校验、WMS 接口（手）
- **执行层**管"给用户看什么"：结果组装、人工确认按钮、合规标注（嘴+印章）
- 快速判别：凡是"调 API"的归工具层；凡是"渲染给用户"的归执行层

### 4. 补充：Skill 兜底层在五层架构的哪里？
它不是单独一层。**由规划层执行**（决定追问/降级/转人工/终止），**由执行层呈现**（降级话术、转人工提示）。是一根贯穿规划层和执行层的保险丝。

---

## 二、逐层拆解（结合项目）

### 1. 感知层——耳朵和眼睛

**一句话**：把"给我弄点靶纸"变成系统认识的 `{意图: 领用, 实体: 靶纸, 置信度: 0.72}`。

| 管什么 | 绝不管 | 怎么交互 |
|---|---|---|
| 语义归一化（别名/口语/指代）、意图识别、置信度判定、拒识 | 怎么执行（规划层的事）、有没有权限（校验卡点） | 上接用户输入，下发结构化 JSON 给规划层 |

**项目对应**：
- 归一化："弄点"→"领用"；"靶纸"→物资编码表标准名（附件层 material-catalog.md 的用途之一）
- 意图边界："领"和"借"一字之差——领用 skill description 里的负面边界，就是这层的判定标准
- 置信度：0.72 落在 0.5~0.85 区间 → 追问"您是要领用还是借用？"

**你要定义的**：
- ⭐ 意图分类体系：建议 9 个——入库、出库、领用、借用、盘点、库存查询、排障问答、保养问答、制度问答
- ⭐ 置信度三档：≥0.85 直接路由；0.5~0.85 追问确认；<0.5 拒识并给统一话术
- ⭐ 拒识与拦截规则：非业务范围说什么、敏感内容怎么拦、统一兜底话术
- ○ 物资别名映射字典：首版只收高频别名，之后靠 bad case 增补

**研发做什么**：选型/微调意图模型、实现归一化算法、对接输入输出接口。

**疑惑 Q&A**
> Q：意图识别不是大模型自动做的吗，我定什么？
> A：模型负责"算"，你负责"裁"——分几类、边界在哪、多少分放行、错了算谁的，全是业务判断。验收方式就是建评测集：把 bad case 沉淀成测试题跑通过率。你项目里"数据评测"干的就是这件事，落点就在感知层。

### 2. 规划层——大脑和调度中心

**一句话**：拿到"领用+靶纸×20"后，决定走领用 skill、缺参数先问谁、校验按什么顺序、错了走哪条降级路。

| 管什么 | 绝不管 | 怎么交互 |
|---|---|---|
| Skill 路由、槽位补全顺序、校验编排、异常决策、意图切换挂起 | 听不听得懂（感知层）、接口怎么调（工具层） | 上接感知层信号，调度工具层，结果交执行层 |

**项目对应（就是你那份领用 skill 的内容）**：
- 路由：意图"领用"→「智慧仓储-物资领用」skill
- 槽位追问：缺 applicant_id 和 purpose → 按定义顺序追问，上限 2 轮
- 校验编排：权限 → 库存 →（箱式查审批单 / 散货查限额）——正是领用 skill"执行逻辑"那四步
- 意图切换：领到一半问"库存还有多少"→ 挂起领用流程，答完问"继续领用吗？"

**关键认知**：**你写的 skill 文件就是规划层的剧本**。skill 的"执行逻辑"= 这层的调度步骤；skill 的"兜底规则"= 这层的异常分支。研发用 LangGraph 做的调度引擎，跑的就是你写的规则。

**你要定义的**：
- ⭐ 意图→Skill 路由表（一一对应）
- ⭐ 每个业务的槽位优先级和追问顺序（缺什么先问什么）
- ⭐ 追问规则：上限几轮、一次问几个、中途改参数怎么办
- ⭐ 校验顺序：先权限、再库存、再品类规则（顺序错了浪费调用还漏风控）
- ▽ 意图切换的挂起/恢复（首版可简化为"中断后重新开始"）

**疑惑 Q&A**
> Q：这不就是一堆 if-else 吗，要大模型干嘛？
> A：规则是确定性的（你定），理解和补参是模型的（它做）。分层意义恰恰是：确定性的事绝不让模型自由发挥。类比：你定审批流的节点和条件，研发做工作流引擎——引擎跑的是你的规则。

### 3. 记忆层——记事本

**一句话**：记住"这个会话进行到哪了"和"这个单位/这个人平时的习惯"。

| 管什么 | 绝不管 | 怎么交互 |
|---|---|---|
| 短期会话记忆（槽位、草稿、对话历史）、长期业务记忆（高频申领、偏好、未归还摘要） | 知识库内容（那是 RAG）、生成正式单据（记忆只能参考） | 供规划层查询上下文；按生命周期过期销毁 |

**项目对应**：
- 短期：本次会话已填的槽位、未提交的领用草稿——30 分钟超时销毁
- 长期：某单位每月初固定领靶纸、某士官偏好自提、名下有未归还借用单——随账号持久化
- 红线（军队场景）：**绝不存 RFID 完整码值、涉密技术参数、审批详情**

**你要定义的**：
- ⭐ 存储范围白名单+黑名单（黑名单是合规底线）
- ⭐ 生命周期：短期 30 分钟过期、长期多久更新、什么情况销毁
- ⭐ 使用规则：记忆只能做推荐参考（"您常领的靶纸本月限额还剩 X"），不能直接生成正式单据
- ▽ 个性化推荐（后置，先跑通基础链路）

**疑惑 Q&A**
> Q：长期记忆到底有什么用？
> A：三个实在场景：①月初主动提醒限额；②借出到期主动催还；③高频物资预填草稿。都是"省一轮追问"，不是炫技。

### 4. 工具层——手脚

**一句话**：封装所有外部能力，核心是管控读写权限——默认全只读，写操作必须人工确认后走业务系统原生接口。

| 管什么 | 绝不管 | 怎么交互 |
|---|---|---|
| 工具清单与分类（只读/写）、入参出参、调用顺序、重试与超时 | 结果怎么展示（执行层） | 被规划层调用；RAG 检索也在这一层（只读工具） |

**项目对应**：
- 只读工具：物资档案查询、库存台账查询、权限校验、库位映射、AGV 状态、历史单据、**RAG 知识检索（排障/保养/制度）**
- 写操作：WMS 出库扣账、门禁指令、AGV 下发——**Agent 不直接执行**；用户在执行层点确认后，由业务系统原生接口执行

**你要定义的**：
- ⭐ 工具清单与分类（每个工具：业务作用、只读还是写、入参出参）
- ⭐ 读写边界：涉密场景默认全只读
- ⭐ 调用规则：顺序、失败重试几次、超时（>5 秒）降级——领用 skill 兜底里"WMS 接口超时转人工"的落点就在这
- ○ 接口规范文档（wms-api-spec.md 已挂进附件层）

**疑惑 Q&A**
> Q：为什么默认全只读？AGV 不是二期就自动了吗？
> A：AGV 自动跑的是确定性流程（门禁+调度系统联动）；Agent 的"写"指大模型触发的业务变更。军队场景写错一次台账就是事故，所以写操作必须过人工确认闸门——责任落在人和流程上，不在模型上。

### 5. 执行层——嘴巴和印章

**一句话**：把结果组装成合规输出，并在任何"盖章"动作前设置人工确认闸门。

| 管什么 | 绝不管 | 怎么交互 |
|---|---|---|
| 输出格式（正常/缺参/异常三类结构）、合规标注、人工确认闸门、展示规范 | 业务逻辑（规划层） | 上接规划层结果，下到用户界面；确认后触发业务系统动作 |

**项目对应**：
- 领用草稿卡片：物资明细 +【确认提交】【修改】【取消】三按钮，标注"AI 生成，仅供参考"
- 门禁扫描结果三栏对比（单据 vs RFID vs 人工清点），差异标红，确认后才触发台账变更
- 统一 JSON 输出（领用 skill"输出格式"定义的字段），前端好渲染、审计好留痕

**你要定义的**：
- ⭐ 输出格式规范（三类结构各含哪些字段）
- ⭐ 人工确认节点清单：哪些操作必须确认（所有写操作）、确认后触发什么、留什么痕
- ○ 前端展示模板（卡片样式、差异标红规则）

**疑惑 Q&A**
> Q：确认按钮不是前端开发的活吗？
> A：前端实现按钮，你定"哪些操作必须经过按钮、确认后触发哪个业务动作、留什么审计字段"。这是业务规则，不是 UI。

### 横切：风控合规层

每层的卡点：感知层拦敏感词与涉密内容；规划层拦越权与超限；工具层拦一切未确认的写调用；执行层拦无确认的最终输出。

你要定：⭐ 审计日志字段标准（谁、何时、哪层、做了什么、结果）；⭐ 密级隔离规则（不同权限能看到什么）。

---

## 三、一次"领 20 张靶纸"，五层视角重走

| 步骤 | 哪一层 | 发生了什么 |
|---|---|---|
| 1 | 感知层 | "帮我领 20 张靶纸"→ 意图=领用，置信度 0.91 |
| 2 | 规划层 | 路由到「物资领用」skill（读它的门牌 description） |
| 3 | 规划层 | 缺领用人/事由 → 按槽位顺序追问 |
| 4 | 规划层+工具层 | 调只读工具：权限校验、库存查询、限额校验 |
| 5 | 执行层 | 组装领用草稿卡片 + 三按钮 + "AI 生成"标注 |
| 6 | 执行层→业务系统 | 用户点确认 → WMS 原生接口出库扣账 → 门禁/AGV（二期） |
| 7 | 风控合规层 | 全程每步写审计日志 |

---

## 四、"感觉要做很多东西"？——分期切片法

**交付物清单 ≠ 同时要做的事**。切三刀：

**第一刀：MVP（三期上线门槛，缺一不可）**
1. 意图分类体系（9 个够用，别贪多）
2. 置信度三档阈值
3. 路由表 + 槽位追问顺序 + 校验顺序
4. 只读工具清单 + 写操作人工确认闸门
5. 输出 JSON 规范
6. 审计日志字段标准

**第二刀：上线后靠 bad case 迭代（你已有的机制）**
- 误调用 → 改 skill 感知层 description
- 参数老错 → 改 skill 执行层输入规范
- 异常失控 → 补兜底规则
- 别名不认识 → 增补别名映射字典
- 置信度不合适 → 调阈值档位

**第三刀：可后置**
- 长期记忆个性化推荐
- 多意图挂起恢复的复杂体验
- 展示规范精细化

**而且你已经完成一部分了**：领用 skill 里已写好执行逻辑（=规划层剧本）、8 条兜底（=异常分支）、输出格式（=执行层规范雏形）、附件挂载（=按需加载）。五个业务 skill 都照领用这个标准写完，规划层和执行层的规则就齐了大半。

---

## 五、层间协议（一次定完，三条）

1. 层间传递一律结构化 JSON，字段固定，禁止大模型自由格式透传
2. 每层携带 userId、sessionId、操作时间——全链路可追溯
3. 任意层报错，统一异常格式回规划层，由规划层决策重试 / 提示用户 / 终止

---

## 六、自测：这 7 个问题都能脱口而出，架构就是你的了

1. 五层的名字和各自比喻？（30 秒内）
2. Agent 感知层和 Skill 感知层的区别？
3. RAG 在五层架构的哪一层？（工具层的只读工具）
4. 记忆层为什么不能直接生成正式单据？
5. 工具层为什么默认全只读？写操作怎么落地？
6. Skill 兜底层由哪层执行、哪层呈现？
7. bad case"把领用识别成借用"该改哪里？（skill 感知层 description 的负面边界）

答不上的翻回对应章节再看一遍——能全答上，这套架构就内化了。

---

## 附：面试 60 秒（原速记 + 项目证据）

"Agent 分层架构我是从产品能力视角主导设计的：感知、规划、记忆、工具、执行五层，外加横切的风控合规层。**感知层**我定了三档置信度阈值（≥0.85 直路由、区间内追问、低于拒识）；**规划层**的调度剧本就是我写的业务 skill——比如物资领用的槽位追问顺序和'权限→库存→限额'校验编排；**记忆层**定了存储黑白名单和生命周期，RFID 完整码值、涉密参数绝对不存；**工具层**默认全只读，写操作必须过**执行层**的人工确认闸门、再由 WMS 原生接口执行；**风控合规层**全链路审计留痕。上线后我们用 bad case 评测集持续迭代——误调用改 description，异常失控补兜底规则。之所以产品主导分层，是把业务规则和合规红线固化到架构里，避免大模型黑盒不可控，涉密仓储场景必须每层有边界、全链路可审计。"
