# 2026-08-29

- 根据 user 老师的企业级 skill 文件方法论（感知层/执行层/兜底层/附件层四层架构），产出：
  - skill万能模板.md：四层架构总览表、可复制模板正文（{{}} 占位符）、各层写法要点、自查清单、面试 30 秒结构化表达
  - skill案例-电商退款申请.md：按模板填写的完整案例（电商客服退款 skill），文末附四层架构映射的教学批注
- user 的真实项目背景（重要，后续 skill/文档均基于此）：湖北某军区智慧仓储系统
  - 客户原痛点：纯纸质台账、人力搬运、轮岗交接断层、偏远库区维修难
  - 三期规划：一期数字化（WMS 出入库/借用/盘点 + RFID）；二期自动化（AGV + 智能门禁）；三期 Agent（自然语言操作仓库）
  - 核心模块：入库、出库、领用、借用、盘点；RAG 知识库：故障排障、设备保养规范、仓储业务制度
  - 物资两类：箱式货物（侦察雷达、光电观测仪等制式精密器材，一箱一套）；散货（营具耗材、靶纸、工具等低价值）
  - 已上线，持续做 bad case 迭代与数据评测；客户认可，后续智能靶场、装备库有复用意向
- 产出 skill-物资领用（智慧仓储）.md：按四层架构编写的物资领用 skill，关键设计：领用与借用意图边界、箱式/散货品类分流（审批单 vs 月度限额）、身份权限前置核验、门禁/AGV 二期联动、多编码匹配时列候选防误操作
- 讲解了 Agent 整体流程（两张 SVG：五层×项目落地架构图、领用请求执行流程图）
- user 桌面有《🐳Agent 分层架构设计🦋.md》（五层：感知/规划/记忆/工具/执行 + 横切风控合规层，含 PM vs 研发分工），基于它产出 Agent五层架构学习手册.md：
  - 解开三个同名不同物混淆：Agent 感知层≠Skill 感知层（前台 vs 门牌）；记忆层≠RAG（RAG 属工具层只读工具）；工具层≠执行层（手 vs 嘴+印章）
  - 核心认知：skill 文件就是规划层的剧本（执行逻辑=调度步骤，兜底=异常分支）
  - 交付物分期切片：MVP 六项（意图体系/置信度三档/路由表/只读工具+确认闸门/输出规范/审计字段）vs bad case 迭代 vs 可后置
  - 含 7 题自测清单和面试 60 秒版（附项目证据）
  - user 的学习诉求：想把架构变成自己的知识，曾因环节多感到疑惑
- user 背景补充：前端开发转岗 AI 产品经理（重要：解释需求时多用前端类比——接口契约/测试用例/联调分工）
- 问答1（感知层产出什么）：产出=判定标准+数据资产+验收机制（意图体系/阈值/评测集/别名字典），不是提示词也不是 skill；"意图识别用什么框架"五段式答法（结论→约束→演进→证据→分工）
- 问答2（个人工作流程）：产出 AI产品经理项目全记录-故事版.md——第一人称剧本，8 幕（进山调研→三期说服→一期状态机→二期AGV/三栏对比雏形→三期角色边界→bad case 评审会→客户复盘会→六字工作流"听切定给验讲"），含面试三冲突场景讲法和产出物清单
- 问答3（prompt 相关三连问）：① prompt 分布在 6 个节点（系统/意图识别/参数抽取/RAG 问答/回复生成/skill description），原则=内容（说什么）PM 定、工程（怎么稳定做到）算法做；铁律=规则只写在 skill 一处、prompt 引用不复制；② 小公司 PM 写 prompt 很常见，分界线在顺序——先标准后翻译成 prompt 才是 PM 思维，直接调 prompt 是玄学调参；③ 意图≠skill：9 意图分三去向（5 业务操作→skill、1 查询→工具直调、3 知识问答→RAG），skill 数量由流程复杂度决定（状态变更+多步校验+兜底）
- 问答4（prompt 怎么写）：产出 Prompt编写手册-六节点模板.md——通用骨架五段（角色/任务/规则/示例/输出格式 ↔ PRD 的定位/用户故事/红线/验收用例/接口契约）；6 节点各含模板+智慧仓储填好示例（系统 prompt"小储"、意图识别含正例/边界例/反例、参数抽取禁编造、RAG 拒答话术、回复生成话术表）；协作流程=PM 写内容需求单→算法组装→PM 评审三检查点→评测验收→bad case 改需求单不改 prompt；新手六坑（散文式/规则写两处/只给正例/没定输出格式/一次改多变量/弱约束词）
- 问答5（模型怎么执行/触发）：核心认知=模型不执行任何东西，只读上下文返回文本，一切动作是代码干的；触发机制=Function Calling（模型输出工具调用 JSON，代码解析→加载 skill 正文/调 API→结果追加上下文→再调模型，循环到纯文本）；前端类比=模型是无状态纯函数 f(context)→output，会话状态靠代码每次全量重传；两张图：Agent 运行时循环图、领靶纸四轮调用的可交互 stepper（每轮上下文包变化：意图索引→skill 正文懒加载→工具结果追加→输出格式）
