# 智慧仓储 Agent 吃透手册

> 目标：读完能讲清本项目 Agent 的完整运转，并能用同一套方法设计其他场景的 Agent。  
> 写法：每个概念都放在"领 20 张靶纸"这条真实链路上讲；每个设计都回答"没有它会出什么事故"——事故记忆比定义记忆牢。



---

## 〇、先建立的唯一认知

大模型不"执行"任何东西。它是一个**无状态函数**：读入上下文，输出文本。它不记得上一轮说过什么，也操作不了任何系统。

Agent 系统的真实组成 = **编排层代码 + 模型 + 一组受控资料（提示词 / skill / 知识库）+ 工具**。运行过程是代码反复调用模型的循环：

> 组装上下文 → 调模型 → 模型返回【纯文本】则结束 / 返回【工具调用】→ 代码执行工具 → 结果写回上下文 → 再次调模型 → …… → 直到模型返回纯文本给用户。

**记忆锚：模型负责判断和表达，代码负责执行和记录。**

✋ 自测：模型能直接修改库存吗？为什么？

---

## 一、上下文：每轮调用的四件套

每一轮发给模型的上下文由编排代码组装，固定四部分：

1. **系统提示词**：角色、能力范围、行为红线（不直改库存、缺参必须追问、超范围拒识）
2. **skill 索引**：9 个意图的 name + description（各 ≤100 字），**每轮常驻**
3. **对话历史**：含此前各轮的工具执行结果
4. **用户新输入**

**记忆锚：常驻的只有守则和门牌，其余全部按需进上下文。**

为什么门牌必须 ≤100 字：上下文每轮全量重传、重新计费——9 个门牌是固定开销，字数既是钱，也是幻觉源（门牌含糊，模型就会选错路）。

✋ 自测：skill 正文为什么不放进常驻索引？

---

## 二、模型的两种输出与"触发"的本质

模型每轮输出只有两种：

- **纯文本** → 直接返回用户（含追问、拒识），本轮结束
- **工具调用 JSON** → 编排代码解析执行：把对应 skill 正文注入上下文 / 调 WMS / 检索 RAG 知识库，结果追加进上下文，再次调模型

所谓"触发 skill"，工程上就是：**代码检测到模型输出的工具调用指向该 skill，把正文注入下一轮上下文**。skill 正文懒加载、RAG 按需检索，本质都是控制上下文体积。

✋ 自测：一次"领 20 张靶纸"，模型被调用几次？每轮上下文新增了什么？

---

## 三、四轮慢镜头：领 20 张靶纸

**第 1 轮 · 意图识别**  
上下文 = 系统提示词 + skill 索引 + 用户输入 → 模型返回 `{"intent":"requisition","confidence":0.95}` → 代码判定 ≥0.85，直接路由，加载《物资领用》skill 正文。  
（若输入是"帮我弄点靶纸"，置信度约 0.6，落在 0.5-0.85 区间 → 代码让模型生成确认话术"您是要领用还是借用"——**不猜，是规则要求的**。）

**第 2 轮 · 参数抽取**  
按 skill 输入规范抽槽位：靶纸 ✓、20 ✓、工号 ✗、事由 ✗ → 缺参必须追问（系统提示词与 skill 兜底规则共同约束），模型生成追问话术，用户补充。

**第 3 轮 · 校验执行**  
模型发起工具调用 `stock_query(靶纸)` → **代码调 WMS** 返回库存 500 → 追加上下文 → 模型按 skill 执行逻辑继续校验月度限额 → 通过。

**第 4 轮 · 生成输出**  
按 skill 输出格式返回草稿单 JSON → 前端渲染三按钮 → 用户点【确认提交】后，**代码直接调用 WMS 出库接口**，扣库存、写台账、（二期）门禁放行与 AGV 调度——**最后这一步写操作不经过模型**。

每轮均写审计日志：谁、何时、输入、识别意图、校验项、确认人、执行结果。

✋ 自测：整条链路哪一步真正变更了库存？由谁执行？

---

## 四、每个设计在防什么事故（反推记忆法）

| 设计                  | 防的事故                    |
| ------------------- | ----------------------- |
| 系统提示词行为红线           | 模型答应越权请求（"帮我把库存改成 200"） |
| skill 门牌负面边界（领用≠借用） | 意图混淆导致误路由——**幻觉根源之一**   |
| 置信度三档路由             | 低置信度硬路由                 |
| 缺参禁止编造（null 规则）     | 槽位幻觉                    |
| 工具结果回填上下文           | 无数据时凭参数记忆作答——**幻觉根源之二** |
| 人工确认闸门              | 无审核的库存变更，事故无法追责         |
| 兜底规则（超时 >5 秒降级转人工）  | 依赖故障时模型胡说               |
| 全链路审计日志             | 事后不可追溯                  |

**幻觉只有两个根源：选错了路（意图/路由错），没给数据凭记忆答。** 所有防幻觉设计都在堵这两个口子。

一个分层细节：兜底规则分两类——**模型可执行的**（追问、话术切换、转流程）写在 skill 正文里；**模型管不了的**（超时重试、服务切换）写在编排代码里。面试讲清这个分层，就能和背概念的人拉开差距。

---

## 五、换场景的万能配方（六问）

| # | 配方问题           | 仓储的答案                          |
| - | -------------- | ------------------------------ |
| 1 | 用户输入分几类？边界是什么？ | 9 意图 + ambiguous；领≠借           |
| 2 | 每类怎么处理？        | skill 四件套：输入 / 逻辑 / 输出 / 兜底    |
| 3 | 查什么、动什么？       | 只读工具 vs 写操作（人工确认后由业务系统执行）      |
| 4 | 异常怎么办？         | 条件→动作的兜底规则                     |
| 5 | 什么绝不允许？        | 红线 + 确认闸门 + 审计                 |
| 6 | 怎么验证与迭代？       | 评测集 + 95% 发版闸门 + bad case 归因闭环 |

**配方演示：智能靶场**（下一单真实场景）

- 意图：预约靶位、成绩查询、器材借用（复用现有 skill，改门牌）、设备故障（复用排障 RAG）、成绩更正申请
- 工具：靶位状态与成绩查询（只读）；成绩更正为写操作，必须教练确认
- 红线：成绩数据 AI 无权直接修改——涉及考核公平，比仓储更严
- 评测：老三样，bad case 从射击口语开始收（"这发咋脱靶了"）

**配方不变，换的只是内容。** 这就是"能设计其他场景 Agent"的含义。

---

## 六、终极自测（十问，全答上来就进 245 题）

1. Agent 系统里，模型和编排代码各负责什么？
2. 模型为什么"无状态"？对话连续性靠什么实现？
3. 每轮上下文的四件套是什么？哪些常驻、哪些按需？
4. 模型的两种输出分别流向哪里？
5. "触发 skill"在工程上指什么？
6. 幻觉的两个根源与对应设计？
7. 四轮调用中，哪一轮真正变更了库存？由谁执行？
8. 兜底规则分哪两类？各自落在哪？
9. 为什么写操作必须人工确认？确认之后模型还参与吗？
10. 新场景设计 Agent，开口的前三个问题是什么？

---

## 七、附：循环几次？回复生成何时触发？

**回复生成的触发者永远是编排代码**，触发点是状态机的每个"用户出口"——缺参追问、校验通过出草稿、异常降级，每次"要面向用户说话"的时刻，回复生成规范（话术表+输出格式）生效一次。它不是独立的第 5 个阶段。实现两种：末轮直接产出（规范常驻，最后说话那轮顺带完成）；分离式（业务循环先出内部 JSON，代码再单独发起一次"JSON→话术"调用，用于话术统一管理）。

**循环次数没有固定值**：模型调用次数 = 工单数 + 出口说话数。领用顺利且参数齐全，正文加载后约 2-3 轮；含一次追问共约 4 次模型调用（意图识别 1 次 + 正文后 3 次）。

**次数是设计出来的，三个控制手段**：
1. 工具粒度合并——把"查库存+限额+审批状态"做成一个原子工具"领用前置校验"，一次小票齐活
2. 追问上限——skill 规划规则里写死 ≤2 轮，这是产品定的成本上限
3. 总轮数护栏——编排代码设上限，超限强制转人工；**模型自己停不下来，死循环只能靠代码拦**

面试对应题："一次交互平均多少轮？怎么控制的？"——数字 + 控制手段 + 时延成本动机，六环齐。

---

*与《Agent五层架构学习手册》的对应：感知=意图识别，规划=路由与编排，记忆=上下文与会话管理，工具=接口封装，执行=输出与确认闸门。这份文档讲机制，那份手册讲术语——机制懂了，术语只是换名字。*
