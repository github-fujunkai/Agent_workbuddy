---
name: 电商客服-退款申请处理
description: 处理用户退款申请，核查订单状态并执行退款流程。触发场景：用户明确提出退款申请、赔偿等意图时触发；不适用于物流查询、产品咨询等相关场景。
---

# 电商退款申请处理

## 一、输入规范

- order_id：订单号，必填
- reason：退款原因，必填
- user_id：用户 ID，用于权限校验，可选

## 二、执行逻辑

1. 查询订单状态，确认是否在退款窗口期内
2. 校验退款金额是否超出策略上限
3. 调用退款申请接口，写入退款记录

## 三、输出格式

返回 JSON：

- refund_status：退款状态
- refund_amount：退款金额
- eta：退款预计到账时间

## 四、兜底规则

- 如果 order_id 缺失 → 回复用户"请提供订单号"，不进入退款流程
- 如果退款接口超时（>5 秒）→ 降级返回"退款申请已记录"，转人工处理
- 如果退款金额超策略上限 → 路由人工客服，不自动执行
- 如果用户当前订单状态不支持退款 → 返回原因说明，提示联系人工客服

## 五、参考资料（按需加载，不贴原文件内容）

1. refund-policy.md：退款政策详细条款，仅在用户质疑政策时加载
2. refund-api-spec.md：退款接口文档，仅在需要调试接口时加载

---

## 附：本案例与四层架构的对应关系（教学批注，实际 skill 文件不含此节）

| 四层架构 | 本文件对应位置 | 写法验证 |
|---------|--------------|---------|
| ① 感知层（索引） | frontmatter 的 name + description | name 含场景前缀"电商客服"；description 约 70 字，含能做什么、触发条件、不适用场景 |
| ② 执行层（内容） | 一、二、三节 | 输入规范（3 个参数含必填/可选）、执行逻辑（3 步）、输出格式（JSON 字段），每点一句话 |
| ③ 兜底层 | 第四节，独立成节 | 4 条"条件→动作"，覆盖参数缺失、接口超时、金额超限、状态不支持，无一句解释性散文 |
| ④ 附件层（reference） | 第五节 | 只挂文件名 + 加载条件，未复制任何政策条款或接口字段进正文 |
