# 企业级 Skill 文件万能模板

> **核心设计理念**：先有框架，再有细节。一个 skill 文件本质是一套**分层信息架构**——每一层的信息密度、加载时机、服务的对象完全不同。
> **不多写一个字（省 token），不少写一个字（防幻觉失控）。**

---

## 一、四层架构总览

| 层级 | 别名 | 写在哪里 | 作用 | 加载时机 | Token 策略 |
|------|------|---------|------|---------|-----------|
| ① 感知层 | 索引层 | 头部 `name` + `description` | 让 AI 知道"该不该用我" | 每轮对话常驻 prompt | ≤100 字，字字计较 |
| ② 执行层 | 内容层 | 正文：输入规范 / 执行逻辑 / 输出格式 | 定义能力边界与执行规范 | 命中 skill 时加载 | 每点 1-2 句，零散文 |
| ③ 兜底层 | 内容层（独立成节） | 正文独立章节 | 锁定异常场景下的行为下限 | 命中 skill 时加载 | 条件→动作，一行一条 |
| ④ 附件层 | reference | 文末挂载 | 承载大体积详情 | 按需加载，能不触发就不触发 | 只挂路径，严禁复制内容 |

**为什么必须分层**：企业级 Agent 往往有几十个 skill，每个 skill 的 name 和 description 都会进入 prompt。写得越冗长，基础 token 消耗越大，误调用（幻觉）概率越高——对话还没开始就在烧 token。分层的本质是**让每一层级的信息只服务于对应场景**。

---

## 二、模板正文（复制后替换 `{{}}` 占位符）

````
===== 模板开始 =====

---
name: {{场景-问题域，如：电商客服-退款申请处理}}
description: {{做什么+怎么做，一句话。触发场景：{{明确意图描述}}时触发；不适用于{{相邻易混淆场景}}。全文控制在100字以内}}
---

# {{Skill 中文名}}

## 一、输入规范

- {{param_a}}：{{参数说明}}，必填
- {{param_b}}：{{参数说明}}，必填
- {{param_c}}：{{参数说明，如用于权限校验}}，可选

## 二、执行逻辑

1. {{第一步：查询/校验前置状态（如：是否在允许窗口期内）}}
2. {{第二步：业务规则校验（如：金额是否超策略上限）}}
3. {{第三步：执行核心动作（如：调用接口、写入记录）}}

## 三、输出格式

返回 {{JSON / 固定结构}}：{{字段1（状态）、字段2（金额）、字段3（时间）}}

## 四、兜底规则

> 格式：条件 → 动作。一行一条，只写条件与处置动作，不解释原因。
> 至少覆盖两类：关键参数缺失、外部依赖失败。

- 如果 {{关键参数缺失}} → {{提示用户补充信息}}，不进入流程
- 如果 {{外部依赖失败/超时 > N 秒}} → {{降级话术}}，转人工处理
- 如果 {{金额/权限超策略上限}} → 路由人工，不自动执行
- 如果 {{当前状态不支持}} → 返回原因说明，提示联系人工

## 五、参考资料（按需加载，只挂路径不贴内容）

1. {{xxx-policy.md}}：{{政策/规则详细条款}}，仅在{{用户质疑规则}}时加载
2. {{xxx-api-spec.md}}：{{接口/技术文档}}，仅在{{需要调试或查字段}}时加载
3. {{xxx-template.md}}：{{输出模板}}，仅在{{生成对应产物}}时加载

===== 模板结束 =====
````

---

## 三、各层写法要点

### ① 感知层（name + description）

- **name**：精准定义"在什么场景解决什么问题"，避免只写泛化动作名（写"电商客服-退款申请处理"，不要只写"退款申请处理"），防止与其他 skill 边界模糊。
- **description**：必须同时包含三件事——能做什么、触发条件、**不适用场景（负面边界）**。整体控制在 100 字左右，因为它常驻 prompt，写得冗长就是无脑烧 token。

### ② 执行层（正文三段）

- 输入规范、执行逻辑、输出格式，**每个点 1-2 句话**，不写长篇散文——token 消耗与正文长度直接挂钩。

### ③ 兜底层（必须写，不能省）

- 至少两条逻辑：**关键参数缺失怎么办、外部依赖失败怎么办**。
- 必须单独成节，不能藏在正文里——藏在正文里，Agent 遇到问题时大概率失控，出了问题也没法追责。
- 写法格式：**条件 → 动作**，一行一条，不解释过多原因，只写触发条件和处置动作，让 AI 的行为变得可预期。

### ④ 附件层（reference，按需加载）

- 配置文件、模板、政策条款以 reference 形式挂载，正文只写"请参考 xxx 附件"。
- **严禁把附件内容复制进 skill 正文**——只有真正需要查询时才加载全文，能不触发就不触发。

---

## 四、写完自查清单

- [ ] name 能独立说清"在什么场景解决什么问题"，与其他 skill 无边界模糊
- [ ] description ≤100 字，同时包含：能做什么、触发条件、不适用场景
- [ ] 正文三段（输入/逻辑/输出）每点 1-2 句话，零散文
- [ ] 兜底至少 2 条：参数缺失 + 外部依赖失败，独立成节不藏正文
- [ ] 附件只挂路径 + 加载条件，正文中没有任何一段是从附件复制来的

---

## 五、面试结构化表达（30 秒版）

> 一个优秀的企业级 skill 文件是**四层信息架构**：
> **感知层**用 name/description 做索引，100 字内说清触发条件与负面边界；
> **执行层**用输入规范、执行逻辑、输出格式定义能力边界；
> **兜底层**用"条件→动作、一行一条"的降级规则锁定行为下限；
> **附件层**只挂路径、按需加载。
>
> 产品经理的三大核心价值：
> ① 用精准描述控制索引层的 **token 消耗**；
> ② 用清晰的边界定义控制 **agent 的行为边界**；
> ③ 用兜底/降级策略锁定 **agent 的执行下限**。
>
> 这三件事研发不会主动做，只能由产品经理做。

**面试官常见追问及回应**：

| 追问 | 回应要点 |
|------|---------|
| "为什么不能让研发直接写？" | name/description 定义不清 → AI 误调用（幻觉的根本原因之一）；兜底没写清 → 出问题失控且无法追责；附件不做 reference 设计 → 无脑烧 token 且幻觉率升高。这三件事研发不会主动做。 |
| "skill 文件的业务价值是什么？" | 省 token、跨端跨系统复用。但前提是先有框架再有细节——没有框架，这些价值都不成立。 |
| "description 为什么要写不适用场景？" | 几十个 skill 的索引常驻 prompt，不写负面边界就会误触发、烧 token、产生幻觉。 |
