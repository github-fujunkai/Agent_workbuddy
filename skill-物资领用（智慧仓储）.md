---
name: 智慧仓储-物资领用
description: 处理物资领用申请：核验领用人身份与权限，校验库存与审批，生成出库单并联动门禁/AGV。用户明确提出领用、领取物资意图时触发；不适用于借用（需归还）、归还、盘点、库存查询等场景。
---

# 智慧仓储 · 物资领用

## 一、输入规范

- applicant_id：领用人 ID，关联门禁/身份系统，必填
- unit：所属单位，用于权限与限额校验，必填
- material_name / material_code：物资名称或编码，必填
- quantity：领用数量，必填
- purpose：领用事由，必填

## 二、执行逻辑

1. 核验领用人身份、所属单位及领用权限
2. 查询物资库存与状态，识别品类（箱式货物 / 散货）
3. 品类分流校验：箱式货物核查审批单有效性；散货核查月度领用限额
4. 调用 WMS 生成领用出库单、扣减库存并写入电子台账；第二期环境同步下发门禁放行与 AGV 搬运任务

## 三、输出格式

返回 JSON：

- requisition_id：领用单号
- status：领用状态（已出库 / 待审批 / 已受理）
- details：物资明细（编码、名称、数量、品类）
- pickup_info：领取方式（自提出库区位置 / AGV 送达预计时间）

## 四、兜底规则

- 如果物资名称模糊且匹配多个编码 → 列出候选物资请用户确认，不默认取第一个
- 如果 applicant_id 缺失或身份核验失败 → 提示重新核验身份，不进入领用流程
- 如果库存不足 → 返回当前可用库存数，建议调整数量或联系库房管理员，不生成领用单
- 如果箱式货物无有效审批单 → 提示先完成审批流程，不自动出库
- 如果散货超月度限额 → 路由人工审批，不自动执行
- 如果识别为借用意图（需要归还）→ 转借用流程，不按领用处理
- 如果 WMS 接口超时（>5 秒）→ 降级返回"领用申请已记录"，转人工处理
- 如果物资处于锁定/冻结状态（如盘点中）→ 返回原因说明，提示联系库房管理员

## 五、参考资料（按需加载，不贴原文件内容）

1. material-catalog.md：物资编码对照表，仅在物资名称模糊需匹配编码时加载
2. requisition-policy.md：领用制度与审批权限条款，仅在用户质疑领用规则时加载
3. wms-api-spec.md：WMS 领用接口文档，仅在需要调试接口时加载

---

## 附：设计批注（实际部署时删除本节）

| 设计点 | 本文件的体现 | 对应四层 |
|--------|------------|---------|
| 领用 ≠ 借用 | description 负面边界排除借用；兜底层"借用意图转借用流程" | 感知层 + 兜底层 |
| 品类分流 | 执行逻辑第 3 步：箱式查审批单，散货查限额——两类物资管控强度不同 | 执行层 |
| 军队权限管控 | 输入规范 applicant_id + unit 必填，执行逻辑第 1 步先核身份 | 执行层 |
| 三期演进兼容 | 执行逻辑第 4 步注明"第二期环境同步下发门禁/AGV"，一期环境自然跳过 | 执行层 |
| 防幻觉关键设计 | 兜底层第 1 条：多编码匹配时列候选让用户确认，不默认取第一个——这是 agent 误操作的高发点 | 兜底层 |
| 附件按需加载 | 编码表、制度条款、接口文档分别绑定明确的加载条件 | 附件层 |
