# 评测集 & Bad Case 台账模板（智慧仓储 Agent）

> 两张表的关系是一条闭环：**bad case 进台账 → 归因到层 → 修复 → 转为评测集用例 → 回归通过 → 关闭**。
> 台账管过程，评测集管结果；评测集同时是发版闸门的判据。

---

## 一、评测集（回归用）

### 1.1 设计原则

1. **来源优先级**：真实 bad case 沉淀 > 按意图分类体系的边界例构造 > 常规正例——难例优先，防止集子"太简单测不出问题"
2. **分层覆盖**：意图识别层 / 参数抽取层 / 端到端层三层都要有，避免只测意图不测全链
3. **配比要求**：每个意图至少 20 条（正例 10 / 边界例 5 / 反例 5），上线期总量 ≥180 条
4. **防过拟合**：评测集用例禁止直接进 few-shot；few-shot 禁止直接抄评测集——两者必须隔离
5. **动态增长**：每条新 bad case 归因修复后，必须转化为 1 条评测用例（见台账"转评测集"字段）

### 1.2 主表模板

| 字段 | 说明 |
|---|---|
| case_id | TC-序号，全局唯一，永不复用 |
| 层级 | 意图识别 / 参数抽取 / 端到端 |
| 所属意图/环节 | requisition / ambiguous / 领用全流程 … |
| 类型 | 正例 / 边界例 / 反例 |
| 用户输入 | 原始口语，**不修改不润色** |
| 期望意图 | 分类体系文档中的意图代码 |
| 期望槽位/行为 | 抽取字段期望值，或期望的交互行为（追问/拒识/转人工） |
| 难度 | 高 / 中 / 低 |
| 来源 | bad case / 构造 / 真实采集 |
| 入集日期 | — |
| 最近回归结果 | 通过 / 不通过（含日期） |
| 状态 | 有效 / 待复核 / 已淘汰（并注明原因） |

### 1.3 示例行

| case_id | 层级 | 意图/环节 | 类型 | 用户输入 | 期望意图 | 期望槽位/行为 | 难度 | 来源 | 最近回归 |
|---|---|---|---|---|---|---|---|---|---|
| TC-001 | 意图识别 | requisition | 正例 | 帮我领 20 张靶纸 | requisition | 物资=靶纸，数量=20 | 低 | 构造 | 通过 |
| TC-002 | 意图识别 | ambiguous | 边界例 | 帮我弄点靶纸 | ambiguous，候选含领用/借用 | 触发追问确认 | 高 | bad case | 通过 |
| TC-003 | 参数抽取 | requisition | 边界例 | 领 20 张靶纸，三连的 | requisition | material=靶纸，quantity=20张，unit_hint=三连，其余 null 禁编造 | 中 | bad case | 通过 |
| TC-004 | 意图识别 | 拒识 | 反例 | 今天食堂吃什么 | 拒识 + 统一兜底话术 | 不路由任何业务 | 低 | 构造 | 通过 |
| TC-005 | 端到端 | 领用全流程 | 正例 | 多轮：领 20 张 → 补工号事由 → 确认提交 | 生成草稿单 | 库存校验通过 + WMS 出库 + 台账更新 | 中 | 真实采集 | — |

### 1.4 评分规则与发版闸门

- **单条判定**：意图对 + 关键槽位对 + 行为对，三者全对 = 通过；任一错 = 不通过
- **汇总指标**：意图准确率 / 误路由率（高严重度单独统计）/ 拒识正确率 / 端到端通过率
- **发版闸门**：总通过率 **<95%** 或 **误路由率 >2%** → 拦截发版，修复后全量重跑

---

## 二、Bad Case 台账（过程管理）

### 2.1 主表模板

| 字段 | 说明 |
|---|---|
| bc_id | BC-序号 |
| 日期 | 发现日期 |
| 用户 | 脱敏（如"战士A""库管员B"），禁止记录番号 |
| 原始输入 | 用户原话，不改写 |
| 实际行为 | Agent 实际做了什么（误路由 / 参数错 / 幻觉 / 没兜住…） |
| 期望行为 | 应该做什么 |
| 归因层 | 感知 / 规划 / 记忆 / 工具 / 执行 / 数据（RAG） |
| 根因分类 | 见 2.2 速查表 |
| 严重度 | P0 写操作错误或涉密泄露（立即拦版）/ P1 业务流程走错 / P2 体验问题 |
| 修复方案 | 一句话说清改什么 |
| 改哪个文件 | 落到具体文件（如 skill-物资领用 的 description） |
| 责任人 | 归因后指定 |
| 状态 | 待归因 / 修复中 / 已回归 / 已关闭 |
| 转评测集 | 是（TC-xxx）/ 否（并注明原因） |

### 2.2 归因速查表（现象 → 层 → 改哪里）

| 现象 | 归因层 | 改哪里 |
|---|---|---|
| 认错意图（借用→领用） | 感知层 | 意图分类体系加边界规则；few-shot 加边界例 |
| 口语/别名不认识 | 感知层 | 别名映射字典增补 |
| 听懂了但参数抽错/编造 | 执行层·输入规范 | 槽位规则（禁猜测、null 规则） |
| 校验顺序错 / 漏校验 | 规划层 | skill 执行逻辑 |
| 接口报错没兜住 | 工具层 | 调用规则 + 超时降级 |
| 回答格式乱 / 话术自由发挥 | 执行层·输出 | 输出格式 / 话术表 |
| 知识库答非所问 / 不该答的编了 | 数据（RAG） | 分块策略 / 检索 / 拒答规则 |
| 该拒没拒 / 该拦没拦 | 风控卡点 | 对应层的拦截规则 |

### 2.3 示例行

| bc_id | 日期 | 用户 | 原始输入 | 实际行为 | 期望行为 | 归因层 | 根因 | 严重度 | 修复方案 | 改哪个文件 | 状态 | 转评测集 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BC-001 | 08-30 | 战士A | 帮我弄点靶纸 | 直接按领用出账 | 应追问确认借/领 | 感知层 | 边界缺失 | P1 | description 加负面边界 + 三档阈值 | skill-物资领用 | 已回归 | 是→TC-002 |
| BC-002 | 08-30 | 库管员B | 借两套对抗装备 | 误判为领用出账 | 借用流程 | 感知层 | 边界缺失 | P1 | few-shot 加"还"字边界例 | 意图识别需求单 | 已回归 | 是 |
| BC-003 | 09-02 | 战士C | 观测仪报 E03 怎么办 | 走了库存查询 | 检索排障知识库 | 规划层 | 路由规则缺 | P2 | few-shot 加排障示例 | 意图识别需求单 | 修复中 | 否（先修） |
| BC-004 | 09-03 | 战士A | 领 20 张靶纸，三连的 | 数字编造成指定人 | 不确定归属应置 null | 执行层 | 槽位规则缺 | P0 | 输入规范加"禁猜测"规则 | skill-物资领用·输入规范 | 已回归 | 是→TC-003 |
| BC-005 | 09-05 | 库管员B | （触发接口超时） | 直接报错给用户 | 降级转人工话术 | 工具层 | 兜底缺 | P1 | 超时>5秒降级规则 | skill-物资领用·兜底 | 已回归 | 是 |

---

## 三、评审会怎么用这两张表（每周四 30 分钟）

1. **新 case 入台账**（5 min）：本周采集的 bad case 全部建档，禁止口头流转
2. **逐条归因**（10 min）：按 2.2 速查表归到层，归因不到层的不许关闭——挂"待归因"下周必须有结论
3. **定修复方案**（10 min）：每条指定"改哪个文件"和责任人；PM 改标准和文档，算法改实现
4. **回归与转化**（5 min）：已修复项跑评测集；通过后**必须转化为评测用例**再关闭

**两条铁律**：归因不到层不许关闭；修复不转评测集用例不许关闭。

## 四、与其他文档的联动

- 《意图分类体系-智慧仓储.md》= 评测集"期望意图"的**唯一判定标准来源**
- 台账修复若改动了意图定义 → 分类体系文档版本 +1 → 全量回归
- 评测集回归结果 = 发版闸门判据（见《面试准备作战计划》中的"闸门拦版"故事——面试可讲的机制细节）

## 五、变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-30 | 首版：评测集主表 + 台账主表 + 归因速查 + 评审会流程 |
