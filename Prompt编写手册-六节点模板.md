# Prompt 编写手册：6 个节点的模板与示例（智慧仓储版）

> 写给第一次要碰 prompt 的你。先记住两件事：
> 1. **协作形式**：你交给算法的不是 prompt 成品，是"内容需求单"；prompt 由算法组装。但你要会写骨架和示例，否则提不出需求、验不了收
> 2. **通用骨架**：任何 prompt 都逃不出五段——角色（=产品定位）、任务（=用户故事）、规则（=业务红线）、示例（=验收用例）、输出格式（=接口契约）。写 PRD 的你，已经会一半了

---

## 一、六节点逐个拆：模板 + 填好的示例

每个节点三部分：**骨架模板**（可抄）→ **填好的示例**（你的仓库）→ **分界**（你填什么 / 算法加工什么）。

### 1.1 系统 Prompt（全局唯一一份）

**模板骨架：**

```
# 角色
你是{系统名}的{助手定位}，服务{用户群}。通过{交互方式}帮助用户完成{能力范围}。

# 行为准则
1. {权限边界：能碰什么、绝不能碰什么}
2. {模糊处理：意图不明时怎么办}
3. {规则来源：按 skill 执行，不自行解释业务规则}
4. {范围外处理：拒识话术}
5. {安全红线：涉密拦截}

# 输出原则
- {格式来源}
- {追问原则}
- {异常话术来源}
```

**填好的示例（智慧仓储助手"小储"）：**

```
# 角色
你是某部智慧仓储系统的业务助手"小储"，服务对象为库房管理员和基层官兵。
你通过自然语言帮助用户完成：入库、出库、领用、借用、盘点五类业务操作，
以及库存查询、设备排障/保养/制度知识问答。

# 行为准则
1. 必须通过业务 skill 定义的流程执行操作；禁止绕过校验直接生成结果
2. 禁止直接修改任何库存数据；所有写操作只能生成草稿单，
   经用户点击确认后由业务系统执行
3. 用户意图不明或参数缺失时，必须先追问；禁止臆测补全
4. 与仓储业务无关的请求，统一回复："我只能协助仓储业务相关的问题"
5. 涉及涉密内容的请求，一律拒绝并记录日志

# 输出原则
- 操作类结果按各 skill 定义的 JSON 结构输出
- 追问时一次只问一个最关键的缺失参数
- 异常一律使用兜底规则中的标准话术，禁止自行解释原因
```

**分界：** 你填——五条红线、拒识话术、追问原则（全是业务判断）；算法加工——段落顺序与 token 预算、防注入加固、与其他 prompt 的拼装方式。

### 1.2 意图识别 Prompt

**填好的示例：**

```
# 任务
判断用户输入属于以下哪个意图，输出意图代码与置信度。

# 意图清单
- requisition 领用：从库存领取消耗性物资，出账、无需归还
- lend_out 借用：临时取用制式器材，须按期归还、记入在册
- inbound 入库：新物资验收入库
- outbound 出库：物资出库发放（非领用场景）
- inventory_check 盘点：清点核对库存
- stock_query 库存查询：查库存数量/位置/状态，只读
- fault_qa 排障问答：设备故障如何处理
- maintenance_qa 保养问答：设备保养规范
- policy_qa 制度问答：仓储规章制度
- ambiguous 歧义：无法确定时输出

# 示例
输入："帮我领20张靶纸"
输出：{"intent":"requisition","confidence":0.95}
输入："借两套对抗装备，下月5号还"
输出：{"intent":"lend_out","confidence":0.93}
输入："帮我弄点靶纸"
输出：{"intent":"ambiguous","confidence":0.45,"candidates":["requisition","lend_out"]}
输入："雷达还剩几台"
输出：{"intent":"stock_query","confidence":0.91}
输入："今天食堂吃什么"
输出：{"intent":"ambiguous","confidence":0.10}

# 输出格式
仅输出 JSON：{"intent":"代码","confidence":0.00,"candidates":[]}
```

**分界：** 你填——每个意图的定义和边界（"领用 vs 借用"的定义就是你的核心产出）、全部示例（注意三种都要有：正例、边界例"弄点"、反例"食堂"）；算法加工——示例配比与排序、结构化输出约束（JSON mode）、置信度校准。

### 1.3 参数抽取 Prompt

**填好的示例：**

```
# 任务
从用户输入中抽取领用业务所需参数。

# 槽位定义
- applicant_id 领用人ID，必填
- material_name 物资名称，必填
- quantity 数量，必填
- purpose 领用事由，必填

# 规则
1. 未提及的槽位输出 null，禁止编造
2. 数量含单位时保留原始表述
3. 无法确定归属的数字不做猜测

# 示例
输入："帮我领20张靶纸，三连的"
输出：{"applicant_id":null,"material_name":"靶纸","quantity":"20张","purpose":null,"unit_hint":"三连"}

# 输出格式
仅输出 JSON，字段与槽位定义一致
```

**分界：** 你填——槽位清单（就是 skill 的输入规范，你已经写过）、"禁止编造"规则；算法加工——schema 类型校验、字段缺失的重试策略。

### 1.4 RAG 问答 Prompt

**填好的示例：**

```
# 任务
仅基于 <Context> 中的知识库片段回答用户问题。

# 规则
1. 只允许引用 <Context> 内容作答，禁止使用自身知识补充
2. <Context> 中没有答案时，回复："知识库中未找到该问题的处理方法，
   已记录，建议联系装备维修值班室"
3. 回答末尾标注出处文档与章节

# 示例
<Context>：E03 故障表示光电观测仪电源模块异常。处理方法：更换备用
电源模块后重启；若仍报错，返厂维修。
问题："观测仪报 E03 怎么办"
回答：E03 表示电源模块异常。处理：更换备用电源模块后重启；若仍
报错则返厂维修。（出处：设备排障手册 4.2 节）
```

**分界：** 你填——拒答话术、出处标注要求、"不许编"红线；算法加工——检索策略、片段拼接、Context 长度控制。

### 1.5 回复生成 Prompt

**填好的示例：**

```
# 任务
将执行结果 JSON 转为用户回复。

# 规则
1. 成功：一句话确认 + 关键信息（单号/数量/领取方式），不超过两句
2. 缺参：只追问缺失参数，格式："请提供{参数名}"
3. 异常：使用下方话术表，禁止自行解释或承诺

# 话术表
- 库存不足：当前{物资}库存 {N} 件，无法满足本次申请；可调整数量
  或联系库房管理员
- 接口超时：您的申请已记录，稍后由值班员处理，无需重复提交
- 超出限额：本月该项领用已达限额，已为您转人工审批
```

**分界：** 你填——话术表全部内容（每类一句，不许发挥）；算法加工——模板变量绑定、和前端卡片渲染的对接。

### 1.6 Skill description（name + description）

这就是你已经写过的《物资领用》skill 头部那两行——**这个节点的成品直接由 PM 产出**，不经过算法。写法见《skill-物资领用（智慧仓储）.md》。

---

## 二、协作流程：需求单怎么流转

| 步骤 | 谁 | 动作 | 产物 |
|---|---|---|---|
| 1 | PM | 写内容需求单：意图清单+定义+正反例、槽位、话术、红线、格式 | 需求单（一页表/一份文档） |
| 2 | 算法 | 组装成 prompt：加结构化输出约束、防注入、控 token | prompt 成品 |
| 3 | PM | 评审：对照需求单逐条检查"翻译是否忠实" | 评审意见 |
| 4 | 双方 | 评测集跑分，达标才合并进主干 | 评测报告 |
| 5 | PM | bad case 归因 → **改需求单**（不直接改 prompt），算法照单改 prompt | 需求单新版本 |

**评审三个检查点**（PM 看算法交回的 prompt 时问自己）：
1. 红线都在吗？用的是"必须/禁止"硬约束词吗？
2. 示例三类齐吗（正例/边界例/反例）？
3. 输出格式字段完整吗，模型有没有自由发挥空间？

---

## 三、新手六大坑

1. **散文式 prompt**——不分段不列表，模型抓不住重点。永远用 `# 标题` + 列表
2. **规则写两处**——system prompt 里写一遍、skill 里又写一遍。铁律：规则只在 skill 一处，prompt 引用
3. **只给正例**——"帮我领20张靶纸"好识别，难的是"帮我弄点靶纸"。边界例和反例才是效果的分水岭
4. **没定输出格式**——模型每次返回格式都不一样，下游解析必炸。永远用 JSON + 固定字段
5. **一次改多个变量**——改 prompt 一次只改一处，改完跑评测集对比，否则不知道哪处起作用
6. **弱约束词**——"尽量不要""最好"模型不当真。红线必须用"禁止/必须"

---

## 四、检验标准（写完自测）

**每条规则都能对应评测集里至少一条 case**——写"超限额转人工"这条规则时问自己：评测集里有"超限额"的测试题吗？没有就补一条。不可测试的规则就是装饰品。

## 五、一个心法

不会写 prompt 就先写需求单——写需求单的过程会逼你想清楚每条规则、每个边界、每句话术。需求单写顺了，"翻译"成 prompt 只是排版问题。

**你缺的从来不是 prompt 技巧，是把规则想清楚的习惯。**

---

## 六、常见架构疑问：意图识别要不要单独一个 prompt 节点？

**先分清两个层面**：
- **文件组织**：意图清单、边界规则、few-shot 示例写在哪个文档维护——答案是《意图分类体系》文档，它是唯一事实来源，这条不变
- **运行时结构**：是否存在一次独立的"意图识别调用"——这才是分歧点，两种架构都成立，取决于约束

**架构 A · 独立意图节点（本项目采用）**
输入 → 意图识别调用（守则精简版 + 意图清单 + few-shot）→ 输出 `{intent, confidence}` → 代码按三档路由 → 命中后加载 skill 正文进业务调用。
- 优势：意图可**单独评测**（准确率/误路由率）、bad case 可**归因到层**、置信度三档可代码化、未来可换小模型前置
- 代价：多一次串行调用；多轮意图切换需要挂起/恢复机制

**架构 B · 融合式单 Prompt（轻量 MVP 可选）**
角色守则 + 业务规则 + 工具清单全在一个系统 prompt 里，模型在循环中自选工具——"选工具≈意图识别"，无独立判定环节。
- 优势：少一次调用、多轮意图切换自然、实现简单
- 代价：意图无法单独评测、bad case 归因糊、置信度难拿到、边界规则与业务规则互相稀释、无法小模型前置

**判据**：需要"意图独立评测 + 三档置信度路由 + 演进到小模型"→ 必须 A；纯 MVP 快速验证、意图少而浅 → B 可接受，后续再拆。

**一句话备忘**：本文档的"6 个节点"描述的是架构 A 下的 prompt 位置；架构 B 里意图识别节点会消失，但《意图分类体系》文档依然必须存在——它的示例会被塞进系统 prompt 的工具描述里，单一事实来源原则不变。
